# 项目根目录环境变量示例（请复制为 .env 并填写真实值）
# 本次修改：同步 SAM 权重默认位置到 backend/app/checkpoints/
# -----------------------------------------------------------------
# 作用：
# - 为后端提供敏感配置（豆包 API Key、模型 ID、SAM 模型等），避免硬编码到代码中；
# - settings.py 会自动加载本文件（若命名为 .env），未设置的键使用内置默认值。
# - 请勿将真实密钥提交到代码仓库，建议将 .env 加入 .gitignore。

# 前端跨域来源（可保持默认）
FRONTEND_ORIGIN=http://localhost:5174

# Doubao Ark 多模态配置
# - ARK_API_KEY：豆包方舟平台的 API Key（必填）
# - ARK_BASE_URL：地域端点，默认北京（如需切换请按官方文档修改）
# - DOUBAO_MODEL_ID：具体模型 ID，可按你的方舟控制台替换
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_API_KEY=YOUR_ARK_API_KEY
DOUBAO_MODEL_ID=YOUR_MODEL_ID

# Segment Anything 配置（如有 GPU 可改为 cuda）
SAM_MODEL_TYPE=vit_b
SAM_CHECKPOINT_PATH=backend/app/checkpoints/sam2_hiera_base_plus.pt
SAM_DEVICE=cpu

# 可选：以逗号分隔允许的前端来源（优先级高于 FRONTEND_ORIGIN）
FRONTEND_ORIGINS=http://localhost:5174,http://127.0.0.1:5174,http://localhost:5175

# ========================================
# MySQL 数据库配置（可选）
# ========================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name

# ========================================
# JWT 认证配置
# ========================================
JWT_SECRET_KEY=your-secret-key-please-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# ========================================
# 阿里云短信服务配置（可选）
# ========================================
ALIBABA_CLOUD_ACCESS_KEY_ID=your-aliyun-access-key-id
ALIBABA_CLOUD_ACCESS_KEY_SECRET=your-aliyun-access-key-secret
SMS_SIGN_NAME=your-sms-sign-name
SMS_TEMPLATE_REGISTER=SMS_XXXXXX
SMS_TEMPLATE_RESET_PASSWORD=SMS_XXXXXX
VERIFICATION_CODE_EXPIRE_MINUTES=5
VERIFICATION_CODE_LENGTH=6

# 其它可选项（如需）
# LOG_LEVEL=info
